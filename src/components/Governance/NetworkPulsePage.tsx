import React from 'react';
import { NetworkPulse } from './NetworkPulse';

export const NetworkPulsePage: React.FC = () => {
    return (
        <div className="np-page-wrapper">
            <NetworkPulse />
        </div>
    );
};
